title: "CAM: contour operation"
description: Implement contouring with inside/outside offsets and lead‑in/out.
area: cam
allowed_paths:
  - engines/cam/src/ops/contour.rs
  - engines/cam/src/offsets.rs
  - engines/cam/tests/
labels:
  - codex
  - cam
  - ops
acceptance:
  commands:
    - cargo test -p cam
  tests_expect:
    - No self‑intersections; path lengths match expectations
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
# references:
# - https://github.com/gnea/grbl
# - https://github-wiki-see.page/m/gnea/grbl/wiki
