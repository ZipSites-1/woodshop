title: "Nest: benchmarks & thresholds"
description: Add micro/macro benches and fail CI when variance exceeds thresholds.
area: nest
allowed_paths:
  - engines/nest/benches/plan_bench.rs
  - engines/nest/Cargo.toml
labels:
  - codex
  - nest
  - bench
acceptance:
  commands:
    - cargo bench -p nest
  tests_expect:
    - Bench variance within ±5% over 5 runs
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
# references:
# - https://doc.rust-lang.org/cargo/reference/workspaces.html
