title: "Nest: best-fit for sheets (2D)"
description: Implement 2D best-fit strategy honoring grain and kerf.
area: nest
allowed_paths:
  - engines/nest/src/strategy/best_fit.rs
  - engines/nest/src/geom.rs
  - engines/nest/tests/
labels:
  - codex
  - nest
  - strategy
acceptance:
  commands:
    - cargo test -p nest
    - cargo bench -p nest
  tests_expect:
    - "Property: utilization monotonicity"
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
# references:
# - https://doc.rust-lang.org/cargo/reference/workspaces.html
