title: "CAM: linking (leads, ramps, safe‑Z)"
description: Add linking moves, helical ramps, and safe‑Z management across ops.
area: cam
allowed_paths:
  - engines/cam/src/linking.rs
  - engines/cam/src/tests/
labels:
  - codex
  - cam
  - linking
acceptance:
  commands:
    - cargo test -p cam
  tests_expect:
    - No rapid collisions; safe‑Z honored
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
# references:
# - https://github.com/gnea/grbl
# - https://github-wiki-see.page/m/gnea/grbl/wiki
