title: "Wood: species DB import & IDs"
description: Import species dataset with stable IDs and versioning.
area: docs-wood
allowed_paths:
  - docs/**
  - engines/wood/**
  - codex/project_overview.md
labels:
  - codex
  - docs
  - wood
acceptance:
  commands:
    - cargo test -p wood || true
  tests_expect:
    - Species database loads; sample calculations match ranges
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
