title: "OCCT: boolean fuzz suite"
description: Create a bounded-complexity fuzz suite for boolean operations with failure reduction and logging.
area: occt-core
allowed_paths:
  - engines/occt-core/tests/fuzz_boolean.cpp
labels:
  - codex
  - occt-core
  - fuzz
acceptance:
  commands:
    - cmake --preset relwithdebinfo
    - cmake --build --preset relwithdebinfo -j
    - ctest --preset relwithdebinfo --output-on-failure
  tests_expect:
    - Fuzz suite executes with zero unexpected crashes
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
# references:
# - https://dev.opencascade.org/about/data_exchange
# - https://old.opencascade.com/content/data-exchange
