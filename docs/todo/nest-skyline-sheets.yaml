title: "Nest: skyline strategy (2D)"
description: Implement skyline algorithm for rectangular placement with deterministic tie‑breakers.
area: nest
allowed_paths:
  - engines/nest/src/strategy/skyline.rs
  - engines/nest/src/lib.rs
  - engines/nest/tests/
labels:
  - codex
  - nest
  - strategy
acceptance:
  commands:
    - cargo test -p nest
    - cargo bench -p nest
  tests_expect:
    - Skyline golden layouts stable under fixed seed
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
# references:
# - https://doc.rust-lang.org/cargo/reference/workspaces.html
