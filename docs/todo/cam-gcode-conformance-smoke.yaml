title: "CAM: G‑code conformance smoke"
description: Add a smoke test that runs posted G‑code through a GRBL parser/simulator and checks for alarms.
area: cam
allowed_paths:
  - engines/cam/tests/grbl_smoke.rs
labels:
  - codex
  - cam
  - grbl
acceptance:
  commands:
    - cargo test -p cam
  tests_expect:
    - Smoke passes on samples without alarms
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
# references:
# - https://github.com/gnea/grbl
# - https://github-wiki-see.page/m/gnea/grbl/wiki
