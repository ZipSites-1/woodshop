title: "CAM: simulator/dry‑run collisions"
description: Add a simple simulator to detect collisions and min clearance violations.
area: cam
allowed_paths:
  - engines/cam/src/simulate.rs
  - engines/cam/src/tests/simulate.rs
labels:
  - codex
  - cam
  - sim
acceptance:
  commands:
    - cargo test -p cam
  tests_expect:
    - No collisions for calibrated samples
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
# references:
# - https://github.com/gnea/grbl
# - https://github-wiki-see.page/m/gnea/grbl/wiki
