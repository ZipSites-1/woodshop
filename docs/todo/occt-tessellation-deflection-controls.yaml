title: "OCCT: tessellation deflection controls"
description: Add parameterized deflection and angle thresholds and thread through to the viewer.
area: occt-core
allowed_paths:
  - engines/occt-core/include/woodshop/geom/Tessellate.hpp
  - engines/occt-core/src/geom/Tessellate.cpp
  - apps/web-viewer/src/components/ViewerCanvas.tsx
labels:
  - codex
  - occt-core
  - viewer
acceptance:
  commands:
    - cmake --preset relwithdebinfo
    - cmake --build --preset relwithdebinfo -j
    - pnpm -w --filter @woodshop/web-viewer build
  tests_expect:
    - Toggle deflection and see triangle count delta
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
# references:
# - https://dev.opencascade.org/about/data_exchange
# - https://old.opencascade.com/content/data-exchange
