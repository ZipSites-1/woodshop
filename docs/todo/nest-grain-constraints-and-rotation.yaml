title: "Nest: grain constraints & rotation"
description: Add strict/relaxed/none grain rules; disable rotation when required.
area: nest
allowed_paths:
  - engines/nest/src/geom.rs
  - engines/nest/src/lib.rs
  - engines/nest/tests/
labels:
  - codex
  - nest
acceptance:
  commands:
    - cargo test -p nest
  tests_expect:
    - Rotation disabled when grain=strict
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
# references:
# - https://doc.rust-lang.org/cargo/reference/workspaces.html
