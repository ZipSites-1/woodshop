title: "Desktop: undo/redo & revisions"
description: Record revisions per tool call; implement undo/redo navigation.
area: desktop-app
allowed_paths:
  - apps/desktop/src/**
  - apps/desktop/qml/**
labels:
  - codex
  - desktop
  - qt
acceptance:
  commands:
    - cmake --build --preset relwithdebinfo
    - ctest --preset relwithdebinfo
  tests_expect:
    - "Manual smoke: credenza demo end-to-end"
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
# references:
# - https://turborepo.com/docs/crafting-your-repository/caching
# - https://nx.dev/concepts/decisions/why-monorepos
