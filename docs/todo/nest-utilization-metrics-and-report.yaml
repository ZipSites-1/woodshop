title: "Nest: utilization metrics & report"
description: Compute % utilization per sheet and a project summary report.
area: nest
allowed_paths:
  - engines/nest/src/lib.rs
  - engines/nest/tests/
labels:
  - codex
  - nest
  - metrics
acceptance:
  commands:
    - cargo test -p nest
  tests_expect:
    - Utilization reported within 0.1% of benchmark
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
# references:
# - https://doc.rust-lang.org/cargo/reference/workspaces.html
