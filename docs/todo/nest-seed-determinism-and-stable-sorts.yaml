title: "Nest: seed determinism & stable sorts"
description: Inject seeded RNG and enforce stable ordering on equal‑score placements.
area: nest
allowed_paths:
  - engines/nest/src/util/seed.rs
  - engines/nest/src/lib.rs
  - engines/nest/tests/
labels:
  - codex
  - nest
  - determinism
acceptance:
  commands:
    - cargo test -p nest
  tests_expect:
    - Two runs with same seed produce bit‑identical layouts
  artifacts_expect:

dependencies:

out_of_scope:

created: 2025-09-21
# references:
# - https://doc.rust-lang.org/cargo/reference/workspaces.html
