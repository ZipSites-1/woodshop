slug: release-changelog-and-tags
title: "Release: changelog & tags"
description: Generate release notes and push annotated tags.
area: ci-release
allowed_paths:
  - packages/**
  - .github/workflows/**
labels:
  - codex
  - ci-release
acceptance:
  commands:
    - echo 'notes + tags'
  tests_expect:

  artifacts_expect:

dependencies:
  - release-changesets-and-semver
github:
  blocked_by:
    - release-changesets-and-semver
  closing_keyword: True
created: 2025-09-21
