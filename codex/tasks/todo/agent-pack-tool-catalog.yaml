slug: agent-pack-tool-catalog
title: "Agent Pack: tool catalog (woodshop-tools.json)"
description: Publish catalog mirrored from MCP registry with JSON Schemas, examples, determinism flags, and side-effect notes.
area: agent-pack
allowed_paths:
  - codex/catalogs/**
  - packages/schemas/**
labels:
  - codex
  - agent-pack
acceptance:
  commands:
    - pnpm -w codegen:schemas
  tests_expect:
    - Catalog validates schema references; names match live registry
  artifacts_expect:

dependencies:
  - mcp-registry-and-discovery

github:
  closing_keyword: True
created: 2025-09-23

