slug: mcp-tool-generate-toolpaths
title: "MCP Tool: generate_toolpaths"
description: Create toolpaths with preview (no G‑code).
area: mcp-server
allowed_paths:
  - apps/mcp-server/src/**
  - packages/schemas/**
  - packages/types/**
labels:
  - codex
  - mcp-server
acceptance:
  commands:
    - pnpm -w build
    - "pnpm -w test:mcp"
  tests_expect:
    - Contract tests pass; schema validation enforced
  artifacts_expect:

dependencies:
  - mcp-registry-and-discovery
  - mcp-schema-validation-middleware
  - cam-contour-operation
  - cam-pocket-operation
  - cam-drill-operation
github:
  blocked_by:
    - mcp-registry-and-discovery
    - mcp-schema-validation-middleware
    - cam-contour-operation
    - cam-pocket-operation
    - cam-drill-operation
  closing_keyword: True
created: 2025-09-21
