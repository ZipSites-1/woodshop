slug: mcp-tool-make-drawing
title: "MCP Tool: make_drawing"
description: Generate views with dimensions and optional explode.
area: mcp-server
allowed_paths:
  - apps/mcp-server/src/**
  - packages/schemas/**
  - packages/types/**
labels:
  - codex
  - mcp-server
acceptance:
  commands:
    - pnpm -w build
    - "pnpm -w test:mcp"
  tests_expect:
    - Contract tests pass; schema validation enforced
  artifacts_expect:

dependencies:
  - mcp-registry-and-discovery
  - mcp-schema-validation-middleware
  - occt-tessellation-deflection-controls
github:
  blocked_by:
    - mcp-registry-and-discovery
    - mcp-schema-validation-middleware
    - occt-tessellation-deflection-controls
  closing_keyword: True
created: 2025-09-21
