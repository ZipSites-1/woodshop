slug: security-mcp-tool-allowlist
title: "Security: MCP tool allow‑list"
description: Per‑tool allow‑list and scoped permissions.
area: security
allowed_paths:
  - apps/mcp-server/**
labels:
  - codex
  - security
acceptance:
  commands:
    - "pnpm -w test:mcp"
  tests_expect:

  artifacts_expect:

dependencies:
  - mcp-registry-and-discovery
github:
  blocked_by:
    - mcp-registry-and-discovery
  closing_keyword: True
created: 2025-09-21
