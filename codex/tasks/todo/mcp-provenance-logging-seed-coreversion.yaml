slug: mcp-provenance-logging-seed-coreversion
title: "MCP: provenance logging"
description: Echo seed, engine versions, revision id; structured logs.
area: mcp-server
allowed_paths:
  - apps/mcp-server/src/**
  - packages/schemas/**
  - packages/types/**
labels:
  - codex
  - mcp-server
acceptance:
  commands:
    - pnpm -w build
    - "pnpm -w test:mcp"
  tests_expect:
    - Contract tests pass; schema validation enforced
  artifacts_expect:

dependencies:
  - mcp-registry-and-discovery
github:
  blocked_by:
    - mcp-registry-and-discovery
  closing_keyword: True
created: 2025-09-21
