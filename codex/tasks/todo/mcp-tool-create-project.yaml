slug: mcp-tool-create-project
title: "MCP Tool: create_project"
description: Implement create_project with tests and examples.
area: mcp-server
allowed_paths:
  - apps/mcp-server/src/**
  - packages/schemas/**
  - packages/types/**
labels:
  - codex
  - mcp-server
acceptance:
  commands:
    - pnpm -w build
    - "pnpm -w test:mcp"
  tests_expect:
    - Contract tests pass; schema validation enforced
  artifacts_expect:

dependencies:
  - mcp-registry-and-discovery
  - mcp-schema-validation-middleware
github:
  blocked_by:
    - mcp-registry-and-discovery
    - mcp-schema-validation-middleware
  closing_keyword: True
created: 2025-09-21
