slug: mcp-tool-apply-joinery
title: "MCP Tool: apply_joinery"
description: Apply joinery; return impacted parts.
area: mcp-server
allowed_paths:
  - apps/mcp-server/src/**
  - packages/schemas/**
  - packages/types/**
labels:
  - codex
  - mcp-server
acceptance:
  commands:
    - pnpm -w build
    - "pnpm -w test:mcp"
  tests_expect:
    - Contract tests pass; schema validation enforced
  artifacts_expect:

dependencies:
  - mcp-registry-and-discovery
  - mcp-schema-validation-middleware
github:
  blocked_by:
    - mcp-registry-and-discovery
    - mcp-schema-validation-middleware
  closing_keyword: True
created: 2025-09-21
