slug: desktop-viewer-bridge-occt
title: "Desktop: viewer bridge (OCCT)"
description: Render tessellation from occt-core with selection and measure tools.
area: desktop-app
allowed_paths:
  - apps/desktop/src/**
  - apps/desktop/qml/**
  - apps/desktop/assets/**
labels:
  - codex
  - desktop
  - qt
acceptance:
  commands:
    - cmake --build --preset relwithdebinfo
    - ctest --preset relwithdebinfo
  tests_expect:
    - "Manual smoke: credenza demo end-to-end"
  artifacts_expect:

dependencies:
  - desktop-chat-pane-and-react-trace

github:
  blocked_by:
    - desktop-chat-pane-and-react-trace
  closing_keyword: True
created: 2025-09-21
