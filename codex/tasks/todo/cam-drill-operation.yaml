slug: cam-drill-operation
title: "CAM: drill operation"
description: drill operation
area: cam
allowed_paths:
  - engines/cam/src/**
  - engines/cam/tests/**
labels:
  - codex
  - cam
acceptance:
  commands:
    - cargo test -p cam
  tests_expect:

  artifacts_expect:

dependencies:

github:
  blocked_by:

  closing_keyword: True
created: 2025-09-21
