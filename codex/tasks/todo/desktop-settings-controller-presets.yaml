slug: desktop-settings-controller-presets
title: "Desktop: settings & controller presets"
description: Add settings view for units, paths, and GRBL controller presets; persist to local store.
area: desktop-app
allowed_paths:
  - apps/desktop/src/**
  - apps/desktop/qml/**
labels:
  - codex
  - desktop
  - qt
acceptance:
  commands:
    - cmake --build --preset relwithdebinfo
  tests_expect:
    - Manual: presets persisted and used by postprocessing
  artifacts_expect:

dependencies:
  - desktop-packaging-installers

github:
  closing_keyword: True
created: 2025-09-23

