slug: desktop-action-cards-and-artifacts
title: "Desktop: action cards & artifacts"
description: Show tool inputs/outputs, durations, and artifact links with undo hooks.
area: desktop-app
allowed_paths:
  - apps/desktop/src/**
  - apps/desktop/qml/**
  - apps/desktop/assets/**
labels:
  - codex
  - desktop
  - qt
acceptance:
  commands:
    - cmake --build --preset relwithdebinfo
    - ctest --preset relwithdebinfo
  tests_expect:
    - "Manual smoke: credenza demo end-to-end"
  artifacts_expect:

dependencies:
  - desktop-chat-pane-and-react-trace

github:
  blocked_by:
    - desktop-chat-pane-and-react-trace
  closing_keyword: True
created: 2025-09-21
