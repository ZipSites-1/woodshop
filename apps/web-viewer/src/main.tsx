import React from "react"; import { createRoot } from "react-dom/client";
function App(){ return <div>Web Viewer stub (load OCCT WASM here)</div>; }
createRoot(document.getElementById("root")!).render(<App/>);
